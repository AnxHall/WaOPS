# Bootstrap Worker

Extraia este pacote na raiz do repositório WaOPS.

Instrua o worker:

> Leia `CONTEXT_HANDOFF.md`, `docs/00-master/WAOPS_MASTER_SPEC.md`, `docs/12-engineering/development-rules.md` e os ADRs antes de programar. Esses arquivos são a fonte oficial de contexto. Não introduza comportamento conflitante sem ADR.

## Git sugerido

```bash
git checkout -b docs/waops-architecture
git add .
git commit -m "docs: add complete WaOPS architecture foundation"
git push -u origin docs/waops-architecture
```
